# 📐 Phân Tích Và Thiết Kế Hệ Thống KPDL

**Hệ thống Khai Phá Dữ Liệu với K-Means Clustering**

Phiên bản: 1.0  
Ngày cập nhật: Tháng 1/2026

---

## 📑 Mục Lục

1. [Giới Thiệu](#1-giới-thiệu)
2. [Phân Tích Yêu Cầu](#2-phân-tích-yêu-cầu)
3. [Kiến Trúc Hệ Thống](#3-kiến-trúc-hệ-thống)
4. [Thiết Kế CSDL](#4-thiết-kế-csdl)
5. [Thiết Kế Chi Tiết](#5-thiết-kế-chi-tiết)
6. [Luồng Hoạt Động](#6-luồng-hoạt-động)
7. [Giao Diện Người Dùng](#7-giao-diện-người-dùng)
8. [Thuật Toán K-Means](#8-thuật-toán-k-means)

---

## 1. Giới Thiệu

### 1.1 Mục Đích
Hệ thống KPDL được xây dựng nhằm phân tích và phân cụm sinh viên dựa trên dữ liệu khảo sát thói quen ăn uống, sử dụng thuật toán K-Means Clustering để nhận diện các nhóm sinh viên có đặc điểm tương đồng.

### 1.2 Phạm Vi
- Kết nối và trích xuất dữ liệu từ SQL Server Data Warehouse
- Tiền xử lý và chuẩn hóa dữ liệu khảo sát
- Áp dụng thuật toán K-Means để phân cụm
- Trực quan hóa kết quả và sinh báo cáo tự động
- Lưu trữ kết quả phân cụm về Data Warehouse

### 1.3 Đối Tượng Sử Dụng
- Nhà nghiên cứu giáo dục
- Phòng công tác sinh viên
- Nhà phân tích dữ liệu

---

## 2. Phân Tích Yêu Cầu

### 2.1 Yêu Cầu Chức Năng

| ID | Chức năng | Mô tả |
|----|-----------|-------|
| FR01 | Kết nối DW | Kết nối với SQL Server Data Warehouse |
| FR02 | Load dữ liệu | Trích xuất dữ liệu từ View đã định nghĩa |
| FR03 | Tiền xử lý | Làm sạch, chuẩn hóa, mã hóa dữ liệu |
| FR04 | Chọn thuộc tính | Cho phép chọn các cột để phân tích |
| FR05 | Cấu hình K-Means | Tùy chọn số cụm K hoặc tự động |
| FR06 | Thực thi K-Means | Chạy thuật toán phân cụm |
| FR07 | Hiển thị kết quả | Biểu đồ PCA, thống kê từng cụm |
| FR08 | Sinh báo cáo | Báo cáo phân tích với kết luận |
| FR09 | Lưu kết quả | Export kết quả về DW hoặc CSV |

### 2.2 Yêu Cầu Phi Chức Năng

| ID | Yêu cầu | Mô tả |
|----|---------|-------|
| NFR01 | Hiệu năng | Xử lý được tập dữ liệu đến 10,000 records |
| NFR02 | Khả dụng | Uptime 99% trong giờ làm việc |
| NFR03 | Bảo mật | Kết nối DB qua TLS, không lưu password |
| NFR04 | Khả năng mở rộng | Dễ dàng thêm thuật toán clustering mới |
| NFR05 | Usability | Giao diện thân thiện, không cần IT skills |

### 2.3 Biểu Đồ Use Case

```
                    ┌──────────────────────────────────────┐
                    │           HỆ THỐNG KPDL              │
                    │                                      │
    ┌───────┐      │  ┌─────────────────────────────┐     │
    │       │      │  │     Kết nối Data Warehouse  │     │
    │       │──────│─▶│                             │     │
    │       │      │  └─────────────────────────────┘     │
    │       │      │                                      │
    │       │      │  ┌─────────────────────────────┐     │
    │ USER  │      │  │     Tiền xử lý dữ liệu      │     │
    │       │──────│─▶│                             │     │
    │       │      │  └─────────────────────────────┘     │
    │       │      │                                      │
    │       │      │  ┌─────────────────────────────┐     │
    │       │──────│─▶│     Cấu hình K-Means        │     │
    │       │      │  └─────────────────────────────┘     │
    │       │      │                                      │
    │       │      │  ┌─────────────────────────────┐     │
    │       │──────│─▶│     Xem báo cáo kết quả     │     │
    │       │      │  └─────────────────────────────┘     │
    │       │      │                                      │
    │       │      │  ┌─────────────────────────────┐     │
    │       │──────│─▶│     Lưu/Export kết quả      │     │
    └───────┘      │  └─────────────────────────────┘     │
                    │                                      │
                    └──────────────────────────────────────┘
```

---

## 3. Kiến Trúc Hệ Thống

### 3.1 Kiến Trúc Tổng Quan

```
┌─────────────────────────────────────────────────────────────────┐
│                        PRESENTATION LAYER                        │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    React Frontend                            │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐│ │
│  │  │DWConnect │ │Preprocess│ │ KMeans   │ │AnalysisComponent ││ │
│  │  │Component │ │Component │ │Component │ │                  ││ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘│ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │ HTTP/REST
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        APPLICATION LAYER                         │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    FastAPI Backend                           │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐│ │
│  │  │ app.py   │ │preprocess│ │ kmeans   │ │ conclusion_engine││ │
│  │  │ (Router) │ │  .py     │ │_engine.py│ │       .py        ││ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘│ │
│  │                                                              │ │
│  │  ┌──────────────────────────────────────────────────────────┐│ │
│  │  │              db_connector.py (DW Bridge)                 ││ │
│  │  └──────────────────────────────────────────────────────────┘│ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │ ODBC/pyodbc
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                          DATA LAYER                              │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              SQL Server Data Warehouse                       │ │
│  │  ┌──────────────────┐    ┌──────────────────────────────┐   │ │
│  │  │ vw_KMeans_Input  │    │ Fact_Clustering_Result       │   │ │
│  │  │   (View - IN)    │    │        (Table - OUT)         │   │ │
│  │  └──────────────────┘    └──────────────────────────────┘   │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Technology Stack

| Layer | Technology | Lý do lựa chọn |
|-------|------------|----------------|
| Frontend | React 18 | Component-based, ecosystem mạnh |
| Visualization | Recharts | Tích hợp tốt với React |
| Backend | FastAPI | Async, hiệu năng cao, auto docs |
| Data Processing | Pandas | Xử lý data linh hoạt |
| ML Algorithm | Scikit-learn | Thư viện ML chuẩn công nghiệp |
| Database | SQL Server | Enterprise DW phổ biến |
| DB Connection | pyodbc | Driver chính thức cho SQL Server |

### 3.3 Component Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                        Frontend (React)                       │
│                                                               │
│  ┌───────────┐    ┌───────────┐    ┌───────────┐            │
│  │  App.js   │───▶│   api.js  │───▶│  axios    │────────┐   │
│  │ (Router)  │    │ (Service) │    │           │        │   │
│  └───────────┘    └───────────┘    └───────────┘        │   │
│       │                                                  │   │
│       ▼                                                  │   │
│  ┌───────────────────────────────────────────────────┐  │   │
│  │                   Components                       │  │   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────────┐ │  │   │
│  │  │DWConnector │ │ Preprocess │ │  Analysis      │ │  │   │
│  │  └────────────┘ └────────────┘ └────────────────┘ │  │   │
│  └───────────────────────────────────────────────────┘  │   │
└─────────────────────────────────────────────────────────│───┘
                                                          │
                                  HTTP REST API           │
                                                          ▼
┌──────────────────────────────────────────────────────────────┐
│                        Backend (FastAPI)                      │
│                                                               │
│  ┌───────────┐    ┌───────────────────────────────────────┐  │
│  │  app.py   │───▶│              Routers                  │  │
│  │  (Main)   │    │  /dw/*  /upload  /preprocess  /kmeans │  │
│  └───────────┘    └───────────────────────────────────────┘  │
│       │                         │                             │
│       ▼                         ▼                             │
│  ┌───────────┐    ┌───────────────────────────────────────┐  │
│  │Session Mgr│    │            Engines                    │  │
│  │(In-Memory)│    │  ┌──────────┐ ┌──────────┐ ┌────────┐ │  │
│  └───────────┘    │  │preprocess│ │ kmeans   │ │conclude│ │  │
│                   │  │  .py     │ │_engine.py│ │  .py   │ │  │
│                   │  └──────────┘ └──────────┘ └────────┘ │  │
│                   └───────────────────────────────────────┘  │
│                                    │                          │
│                                    ▼                          │
│                   ┌───────────────────────────────────────┐  │
│                   │         db_connector.py               │  │
│                   │    (SQL Server Connection Pool)       │  │
│                   └───────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. Thiết Kế CSDL

### 4.1 ERD (Entity Relationship Diagram)

```
┌─────────────────────────────────────────────────────────────┐
│                    vw_KMeans_Input (View)                    │
├─────────────────────────────────────────────────────────────┤
│ PK  respondentID      INT           -- ID sinh viên          │
│     Age               INT           -- Tuổi                  │
│     Gender            VARCHAR(10)   -- Giới tính             │
│     weight            FLOAT         -- Cân nặng              │
│     height            FLOAT         -- Chiều cao             │
│     exercise_freq     INT           -- Tần suất tập TD       │
│     fruit_intake      INT           -- Lượng trái cây        │
│     veggie_intake     INT           -- Lượng rau củ          │
│     fast_food_freq    INT           -- Tần suất fast food    │
│     ...               ...           -- Các biến khác         │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ K-Means Processing
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                 Fact_Clustering_Result (Table)               │
├─────────────────────────────────────────────────────────────┤
│ PK  id                INT IDENTITY  -- Primary key           │
│ FK  respondentID      INT           -- Link to input         │
│     cluster_id        INT           -- Assigned cluster      │
│     created_at        DATETIME      -- Timestamp             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Data Dictionary

**Bảng: Fact_Clustering_Result**

| Tên cột | Kiểu dữ liệu | Null | Mô tả |
|---------|--------------|------|-------|
| id | INT IDENTITY | NOT NULL | Khóa chính, tự động tăng |
| respondentID | INT | NOT NULL | ID sinh viên từ dữ liệu gốc |
| cluster_id | INT | NOT NULL | ID cụm được gán (0, 1, 2, ...) |
| created_at | DATETIME | NOT NULL | Thời điểm phân cụm |

### 4.3 SQL Scripts

```sql
-- Tạo bảng kết quả
CREATE TABLE Fact_Clustering_Result (
    id INT IDENTITY(1,1) PRIMARY KEY,
    respondentID INT NOT NULL,
    cluster_id INT NOT NULL,
    created_at DATETIME DEFAULT GETDATE(),
    
    INDEX IX_respondentID (respondentID),
    INDEX IX_cluster_id (cluster_id)
);

-- Stored procedure lưu kết quả
CREATE PROCEDURE sp_SaveClusteringResult
    @respondentID INT,
    @cluster_id INT
AS
BEGIN
    INSERT INTO Fact_Clustering_Result (respondentID, cluster_id)
    VALUES (@respondentID, @cluster_id);
END;
```

---

## 5. Thiết Kế Chi Tiết

### 5.1 Class Diagram - Backend

```
┌────────────────────────────────────────┐
│            DataPreprocessor            │
├────────────────────────────────────────┤
│ - df: DataFrame                        │
│ - columns: List[str]                   │
│ - scaler: StandardScaler               │
├────────────────────────────────────────┤
│ + load_data(data: dict)                │
│ + clean_data()                         │
│ + encode_categorical()                 │
│ + scale_features()                     │
│ + get_processed_data(): DataFrame      │
└────────────────────────────────────────┘
            │
            │ uses
            ▼
┌────────────────────────────────────────┐
│            KMeansEngine                │
├────────────────────────────────────────┤
│ - model: KMeans                        │
│ - n_clusters: int                      │
│ - labels_: ndarray                     │
│ - centers_: ndarray                    │
│ - metrics: dict                        │
├────────────────────────────────────────┤
│ + auto_select_k(X, k_range): int       │
│ + fit(X, k): dict                      │
│ + predict(X): ndarray                  │
│ + get_statistics(): dict               │
│ + get_pca_points(): List[dict]         │
└────────────────────────────────────────┘
            │
            │ uses
            ▼
┌────────────────────────────────────────┐
│          ConclusionEngine              │
├────────────────────────────────────────┤
│ - statistics: dict                     │
│ - metrics: dict                        │
├────────────────────────────────────────┤
│ + generate_conclusions(): dict         │
│ + interpret_cluster(stats): str        │
│ + evaluate_quality(): List[dict]       │
└────────────────────────────────────────┘
            │
            │ uses
            ▼
┌────────────────────────────────────────┐
│            DBConnector                 │
├────────────────────────────────────────┤
│ - connection_string: str               │
│ - conn: Connection                     │
├────────────────────────────────────────┤
│ + connect(): bool                      │
│ + get_views(): List[str]               │
│ + load_view(name): DataFrame           │
│ + save_clusters(results): bool         │
│ + close()                              │
└────────────────────────────────────────┘
```

### 5.2 API Specifications

#### POST /dw/views
**Request:**
```json
{
    "connection_string": "DRIVER={ODBC Driver 18...}"
}
```

**Response:**
```json
{
    "views": ["vw_KMeans_Input", "vw_Students"],
    "tables": ["Fact_Clustering_Result"]
}
```

#### POST /kmeans
**Request:**
```json
{
    "k": 3,
    "auto_k": false
}
```

**Response:**
```json
{
    "fit_info": {
        "k": 3,
        "silhouette_score": 0.234,
        "davies_bouldin_index": 1.56,
        "inertia": 1450.23
    },
    "statistics": {
        "0": {"size": 106, "percentage": 42.4, "mean": [...], "std": [...]},
        "1": {"size": 74, "percentage": 29.6, ...},
        "2": {"size": 70, "percentage": 28.0, ...}
    },
    "clustering": {
        "labels": [0, 1, 2, 0, ...],
        "pca_points": [{"x": 1.2, "y": -0.5, "label": 0}, ...]
    }
}
```

---

## 6. Luồng Hoạt Động

### 6.1 Sequence Diagram - Phân Cụm K-Means

```
┌──────┐          ┌──────────┐          ┌─────────┐          ┌────────┐
│ User │          │ Frontend │          │ Backend │          │   DW   │
└──┬───┘          └────┬─────┘          └────┬────┘          └───┬────┘
   │  1. Open App      │                     │                   │
   │──────────────────▶│                     │                   │
   │                   │  2. GET /dw/views   │                   │
   │                   │────────────────────▶│                   │
   │                   │                     │  3. Query Views   │
   │                   │                     │──────────────────▶│
   │                   │                     │  4. Views List    │
   │                   │                     │◀──────────────────│
   │                   │  5. Views Response  │                   │
   │                   │◀────────────────────│                   │
   │  6. Select View   │                     │                   │
   │──────────────────▶│                     │                   │
   │                   │  7. POST /dw/load   │                   │
   │                   │────────────────────▶│                   │
   │                   │                     │  8. SELECT * FROM │
   │                   │                     │──────────────────▶│
   │                   │                     │  9. Data          │
   │                   │                     │◀──────────────────│
   │                   │  10. Data Response  │                   │
   │                   │◀────────────────────│                   │
   │  11. Configure K  │                     │                   │
   │──────────────────▶│                     │                   │
   │                   │  12. POST /kmeans   │                   │
   │                   │────────────────────▶│                   │
   │                   │                     │  ──────────────   │
   │                   │                     │ │ Run K-Means │   │
   │                   │                     │  ──────────────   │
   │                   │  13. Results        │                   │
   │                   │◀────────────────────│                   │
   │  14. Show Report  │                     │                   │
   │◀──────────────────│                     │                   │
   │                   │                     │                   │
   │  15. Save to DW   │                     │                   │
   │──────────────────▶│  16. POST /dw/save  │                   │
   │                   │────────────────────▶│                   │
   │                   │                     │  17. INSERT INTO  │
   │                   │                     │──────────────────▶│
   │                   │                     │  18. Success      │
   │                   │                     │◀──────────────────│
   │                   │  19. Save Response  │                   │
   │                   │◀────────────────────│                   │
   │  20. Confirmation │                     │                   │
   │◀──────────────────│                     │                   │
```

### 6.2 Activity Diagram

```
    ┌─────────────────┐
    │      START      │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │  Kết nối DW     │
    └────────┬────────┘
             ▼
    ┌─────────────────┐     Thất bại
    │ Kết nối thành   │─────────────┐
    │     công?       │             │
    └────────┬────────┘             ▼
             │ Thành công   ┌──────────────┐
             ▼              │ Hiển thị lỗi │
    ┌─────────────────┐     └──────────────┘
    │  Chọn View      │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │  Load dữ liệu   │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │ Chọn thuộc tính │
    │   phân tích     │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │  Tiền xử lý     │
    │   dữ liệu       │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │ Cấu hình K-Means│
    │  (Auto K / K)   │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │ Chạy K-Means    │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │ Hiển thị báo cáo│
    │  & biểu đồ      │
    └────────┬────────┘
             ▼
    ┌─────────────────┐
    │ Lưu về DW?      │
    └────────┬────────┘
       Có    │     Không
    ┌────────┴────────┐
    ▼                 ▼
┌─────────┐    ┌─────────────┐
│Lưu kết  │    │    END      │
│quả DW   │    └─────────────┘
└────┬────┘
     ▼
┌─────────────┐
│    END      │
└─────────────┘
```

---

## 7. Giao Diện Người Dùng

### 7.1 Wireframes

**Step 1: Kết nối DW**
```
┌─────────────────────────────────────────────────────────┐
│  KPDL - Khai Phá Dữ Liệu                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │  📊 StudentEatingDW                               │ │
│  │  ✓ Đã kết nối                                     │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  Chọn View để phân tích:                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │  ⭐ vw_KMeans_Input  (Recommended)                │ │
│  └───────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────┐ │
│  │     vw_Students                                    │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │          📥 Load dữ liệu & Tiếp tục             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Step 4: Báo Cáo Phân Tích**
```
┌─────────────────────────────────────────────────────────┐
│  [📋 Báo Cáo]  [📈 Biểu Đồ]                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ╔═══════════════════════════════════════════════════╗ │
│  ║  📊 Báo Cáo Phân Tích 3 Nhóm Sinh Viên           ║ │
│  ║     Dựa trên K-Means Clustering                   ║ │
│  ╚═══════════════════════════════════════════════════╝ │
│                                                         │
│  ┌─────────────┬─────────────┬─────────────┐          │
│  │ 250 SV      │ 3 Nhóm      │ 23% Silho.  │          │
│  └─────────────┴─────────────┴─────────────┘          │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 🟢 Lối sống lành mạnh                    42.4%   │ │
│  │    ✅ Ăn uống cân đối, quan tâm dinh dưỡng       │ │
│  ├───────────────────────────────────────────────────┤ │
│  │ 🟡 Vận động cao, ăn chưa điều độ         29.6%   │ │
│  │    ⚠️ Tập thể dục nhiều nhưng ít rau quả         │ │
│  ├───────────────────────────────────────────────────┤ │
│  │ 🔴 Lối sống thụ động                     28.0%   │ │
│  │    ❌ Ít vận động, phụ thuộc người khác nấu      │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │      🗄️ Lưu Kết Quả Về Data Warehouse          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 8. Thuật Toán K-Means

### 8.1 Mô Tả Thuật Toán

**K-Means Clustering** là thuật toán học máy không giám sát, phân chia dữ liệu thành K cụm sao cho các điểm trong cùng một cụm có đặc điểm tương đồng.

### 8.2 Pseudocode

```
Algorithm: K-Means Clustering
Input: X (data), K (number of clusters)
Output: labels[], centroids[]

1. INITIALIZE: Chọn ngẫu nhiên K điểm làm tâm cụm ban đầu
   centroids = random_sample(X, K)

2. REPEAT until convergence:
   
   2.1 ASSIGNMENT STEP:
       For each point x in X:
           label[x] = argmin_k distance(x, centroid[k])
   
   2.2 UPDATE STEP:
       For each cluster k:
           centroid[k] = mean(points where label = k)
   
   2.3 CHECK CONVERGENCE:
       If centroids not changed significantly:
           BREAK

3. RETURN labels, centroids
```

### 8.3 Đánh Giá Chất Lượng

| Chỉ số | Công thức | Ý nghĩa |
|--------|-----------|---------|
| **Silhouette Score** | (b - a) / max(a, b) | Đo độ gắn kết và tách biệt. Giá trị từ -1 đến 1, càng cao càng tốt |
| **Davies-Bouldin Index** | 1/K × Σ max(Ri + Rj / d(ci, cj)) | Đo độ phân tán trong cụm và khoảng cách giữa các cụm. Càng thấp càng tốt |
| **Inertia** | Σ ||x - centroid||² | Tổng bình phương khoảng cách đến tâm. Càng thấp càng tốt |

### 8.4 Chọn K Tối Ưu

Hệ thống sử dụng **Silhouette Method**:

```python
def auto_select_k(X, k_range=(2, 8)):
    best_k = 2
    best_score = -1
    
    for k in range(k_range[0], k_range[1] + 1):
        kmeans = KMeans(n_clusters=k)
        labels = kmeans.fit_predict(X)
        score = silhouette_score(X, labels)
        
        if score > best_score:
            best_score = score
            best_k = k
    
    return best_k
```

---

## 📎 Phụ Lục

### A. Danh Sách Tài Liệu Tham Khảo
1. Scikit-learn Documentation - K-Means Clustering
2. FastAPI Documentation
3. React Documentation
4. Microsoft SQL Server Technical Documentation

### B. Glossary
- **DW (Data Warehouse)**: Kho dữ liệu
- **ETL**: Extract, Transform, Load
- **PCA**: Principal Component Analysis
- **Silhouette Score**: Chỉ số đánh giá chất lượng phân cụm

---

*Tài liệu được tạo bởi KPDL Team - Tháng 1/2026*
